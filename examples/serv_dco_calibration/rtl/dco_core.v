`timescale 1ns / 1ps

// Empty HDL shell. The body is implemented by the SPICE netlist via SpiceBind.
module dco_core (
    input        enable,
    input  [3:0] trim,
    output       osc
);
endmodule
